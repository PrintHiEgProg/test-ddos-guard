# Ошибка подключения к AI: 
# Используется базовый шаблон...
# CI/CD Pipeline для PrintHiEgProg/test-ddos-guard
# Сгенерировано автоматически (fallback mode)

name: CI Pipeline
on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Setup python
      uses: actions/setup-python@v2
      with:
        python-version: '3.9'

    - name: Install dependencies
      run: echo 'Installing dependencies...'

    - name: Run tests
      run: echo 'Running tests...'

    - name: Build project
      run: echo 'Build completed successfully'
